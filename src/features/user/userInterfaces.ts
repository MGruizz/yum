export interface User {
    id: number;
    username:  string;
    email: string;
    descripcion: string;
    foto_perfil: string;
  }

export interface LoginFormValues {
    email: string;
    password: string;
  }

